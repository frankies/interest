{
  "Servers": {
    "PostgreSQL DB": {
      "Name": "PostgreSQL DB",
      "Group": "Servers",
      "Host": "db",
      "Port": 5432,
      "MaintenanceDB": "${POSTGRES_DB:-mydatabase}",
      "Username": "${POSTGRES_USER:-myuser}",
      "SSLMode": "prefer"
    }
  }
}