

buildscript { 
  def propdepsVersion = '0.0.7'

  repositories {
      maven { url 'http://192.168.4.156:8082/content/groups/YNA2.0/'  }
    
  }
  dependencies { 
    classpath "org.springframework.build.gradle:propdeps-plugin:${propdepsVersion}"
  }
}

apply plugin: 'propdeps'

apply plugin: 'maven'
// apply plugin: 'propdeps-eclipse'

 
defaultTasks 'testSiteMesh'

repositories { 
    mavenLocal()
    mavenCentral()
}

configurations { 
}

dependencies {
    compile 'org.sitemesh:sitemesh:3.0.1' 
}

task hello33 {
    doLast {
       ant.importBuild 'hello-build.xml'
    }
} 
  
/**  可以通过引入ant脚本中target产生 gradle的task ***/
//这里设置传入ant 的引用jar 库
ant.properties['refJar'] =  configurations.compile.asPath  
ant.importBuild('test-build.xml') { antTargetName ->
    'test-' + antTargetName //为了防止与gradle中的task命名冲突，这里允许重命名。
}

/**
 测试打印出Ant所有的Target
**/
task testSiteMesh {
    doLast { 
        def antTargetsNames = ant.references.get("ant.targets").collect { it.name }
        println "\nAnt Targets: ${antTargetsNames}\n"  
         ant.echo('===== Finish ======')
        } 
}
